# Build output (root and any nested crate, incl. firmware/ and tools/)
target/
**/*.rs.bk
Cargo.lock
.DS_Store

# Scratch output from running tools/dict-builder in its own directory
# (the firmware's generated dict_data.rs IS committed as vendored data)
/tools/dict-builder/dict_data.rs

# Generated firmware images
*.uf2

# Local firmware secrets (WiFi creds + Anthropic API key). Copy
# firmware/src/config.example.rs -> firmware/src/config.rs and fill in real
# values; the real file must NEVER be committed.
/firmware/src/config.rs

# Brainstorming visual-companion scratch
.superpowers/

# Demo video is hosted on GitHub (linked in README), not committed
site/demo.mp4
